<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="stylesheet" href="/styles/catalog.css" />
  <title>Berries Show</title>
</head>

<body>
  <%- include('../partials/_nav') %>

  <div class="berry_show">
    <h1 style="font-size: xx-large; color: #ffde00;"><%= berry.name %></h1>
    <h3>Berry Id: <%= berry.id %></h3>
    <h4>Firmness: <%= berry.firmness.name %></h4>
    <% berry.flavors.forEach((flavor) => { %>
    <p>Flavor: <%= flavor.flavor.name %></p>
    <p>Potency: <%= flavor.potency %></p>
    <% }) %>
    <h4>Natural Gift Power: <%= berry.natural_gift_power %></h4>
    <h4>Natural Gift Type: <%= berry.natural_gift_type.name %></h4>
    <h4>Size: <%= berry.size %></h4>
  </div>
</body>

</html>